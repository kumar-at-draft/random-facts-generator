name: ARC Demo
on:
  workflow_dispatch:

jobs:
  Test-Runner:
    runs-on: arc-runner-set-local
    steps:
      - run: sleep 30 && echo "🎉 Self-hosted runner is up and running!"
      - run: kubectl --version
